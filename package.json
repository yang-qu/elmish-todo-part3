{
  "name": "elmish-getting-started",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "scripts": {
    "build": "dotnet fable src -o dist -s --run vite build",
    "preview": "dotnet fable src -o dist -s --run vite preview",
    "start": "dotnet fable watch src -o dist -s --run vite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^5.0.10"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}